{
  "name": "decentralized-forum",
  "version": "1.0.0",
  "description": "Decentralized Forum DApp — UZH Blockchain Programming FS26 (Task 12)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:uzheth": "hardhat run scripts/deploy.js --network uzheth_pos",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "hardhat": "^2.28.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.5.0"
  }
}
